import { PureHeroPipePipe } from './pure-hero-pipe.pipe';

describe('PureHeroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PureHeroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
